<security-realm name="SlaveRealm">
                <server-identities><secret value="" /></server-identities>
</security-realm>

<remote host="${jboss.domain.master.address}" port="${jboss.domain.master.port:9999}" username="admin" security-realm="SlaveRealm"/>
